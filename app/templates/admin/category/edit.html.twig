{% extends '_layouts/_admin.html.twig' %}

{% block title 'Edit Category' %}
{% block h1 'Edit Category' %}

{% block body %}
	<a href="{{ path('admin_category_index') }}" class="link-secondary">back to list</a>
	<div class="container pb-5">
	<div class="d-flex justify-content-end">
		{{ include('admin/category/_delete_form.html.twig') }}
	</div>
		{{ include('admin/category/_form.html.twig', {'button_label': 'Update'}) }}
	</div>
{% endblock %}
